# Cliff vs Vesting

A cliff lock releases the full remaining amount after the unlock time.

A vesting lock releases tokens gradually between the start time and end time after the cliff is reached. The vesting interval defines how often additional tokens become claimable.

Locks can only be extended. Users cannot reduce lock duration or change the locked token/LP address.
