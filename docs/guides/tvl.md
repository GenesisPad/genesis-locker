# TVL Calculation

The smart contract tracks raw token amounts. The API calculates USD TVL, LP value, token value, locked percentage, token metadata, LP pair metadata, and warnings.

LP TVL should be calculated from pair reserves and the percentage of LP supply represented by locked LP tokens. Token TVL should use indexed token price data where available.
