# Genesis Locker Frontend

React/Vite frontend for creating locks, searching token or LP status, showing public lock proof pages, stats, and API integration details.

## Commands

- `npm run dev`
- `npm run build`
- `npm run preview`

Set the API base URL through your deployment environment once the backend is live.
